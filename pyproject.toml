[project]
name = "beat-fulfillment-rate-sms-sending"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "pyodbc>=5.2.0",
    "python-dotenv>=1.1.0",
    "requests>=2.32.0",
]
